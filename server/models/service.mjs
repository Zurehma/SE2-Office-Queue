/**
 *
 * @param {*} name
 * @param {*} code
 * @param {*} averageTime
 */
function Service(name, code, averageTime) {
  this.name = name;
  this.code = code;
  this.averageTime = averageTime;
}

export default Service;
