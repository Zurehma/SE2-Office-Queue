/**
 *
 * @param {*} name
 * @param {*} code
 * @param {*} averageTime
 */
function Service(id, name, code, averageTime) {
  this.id = id;
  this.name = name;
  this.code = code;
  this.averageTime = averageTime;
}

export default Service;
